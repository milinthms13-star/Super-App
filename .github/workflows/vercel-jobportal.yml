name: Vercel JobPortal Deploy

on:
  pull_request:
    paths:
      - "src/modules/jobportal/**"
      - "backend/routes/jobportal.js"
      - "backend/models/Job*.js"
      - "backend/models/EmployerProfile.js"
      - "vercel.json"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/vercel-jobportal.yml"
  push:
    branches:
      - main
    paths:
      - "src/modules/jobportal/**"
      - "backend/routes/jobportal.js"
      - "backend/models/Job*.js"
      - "backend/models/EmployerProfile.js"
      - "vercel.json"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/vercel-jobportal.yml"

jobs:
  preview:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install -g vercel@41.7.3

      - name: Pull Vercel environment
        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build deployment artifact
        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy preview
        id: deploy
        run: |
          URL=$(vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }})
          echo "preview_url=$URL" >> "$GITHUB_OUTPUT"

      - name: Print preview URL
        run: echo "JobPortal preview URL: ${{ steps.deploy.outputs.preview_url }}"

  production:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install -g vercel@41.7.3

      - name: Pull Vercel production environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build production artifact
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy production
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
