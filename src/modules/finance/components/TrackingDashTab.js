import React from "react";

const TrackingDashTab = ({ role, userDashboard, consultantDashboard, adminDashboard, commissionDashboard, auditLogs }) => {
  // TODO: Render dashboards and audit logs based on role
  return <div>Tracking Dashboard Tab (UI coming soon)</div>;
};

export default TrackingDashTab;
