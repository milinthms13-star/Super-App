import React from "react";

const ApplyLeadTab = ({ form, onChange, onSubmit, state, documents, onDocumentUpload }) => {
  // TODO: Render lead application form, document upload, and consent
  return <div>Apply Lead Tab (UI coming soon)</div>;
};

export default ApplyLeadTab;
